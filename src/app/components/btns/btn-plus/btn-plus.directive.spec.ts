import { BtnPlusDirective } from './btn-plus.directive';

describe('BtnPlusDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnPlusDirective();
    expect(directive).toBeTruthy();
  });
});
